import QtQuick
import QtQuick.Controls

QtObject {
    id: root

    property var controlWindow: Window {
        visible: true
        width: 1080
        height: 640
        title: "Mono Dream"
    }
}
